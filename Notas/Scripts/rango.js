function pruebam1(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0) {

    }
    else {
        alert('fuera de rango');

        documen.getElementById('m1').value = '';

    }
}

function pruebam2(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0) {

    }
    else {
        alert('fuera de rango');

        documen.getElementById('m2').value = '';

    }
}
function pruebam3(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0) {

    }
    else {
        alert('fuera de rango');

        documen.getElementById('m3').value = '';

    }
}
function pruebac1(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0) {

    }
    else {
        alert('fuera de rango');

        documen.getElementById('c1').value = '';

    }
}
function pruebac2(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0) {

    }
    else {
        alert('fuera de rango');

        documen.getElementById('c2').value = '';

    }
}
function pruebac3(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0) {

    }
    else {
        alert('fuera de rango');

        documen.getElementById('c3').value = '';

    }
}
function pruebal1(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0) {

    }
    else {
        alert('fuera de rango');

        documen.getElementById('l1').value = '';

    }
}
function pruebal2(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0) {

    }
    else {
        alert('fuera de rango');

        documen.getElementById('l2').value = '';

    }
}
function pruebal3(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0) {

    }
    else {
        alert('fuera de rango');

        documen.getElementById('l3').value = '';

    }
}
function pruebat1(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0) {

    }
    else {
        alert('fuera de rango');

        documen.getElementById('t1').value = '';

    }
}
function pruebat2(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0) {

    }
    else {
        alert('fuera de rango');

        documen.getElementById('t2').value = '';

    }
}
function pruebat3(n) {
    var num = n.value;
    if (parseFloat(num) >= 0.0 && parseFloat(num) <= 10.0) {

    }
    else {
        alert('fuera de rango');

        documen.getElementById('t3').value = '';

    }
}